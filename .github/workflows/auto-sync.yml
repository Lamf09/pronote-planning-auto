name: Pronote Planning Auto-Sync

on:
  schedule:
    - cron: '0 */3 * * *'  # Toutes les 3h
  workflow_dispatch:  # Lancement manuel

jobs:
  sync:
    runs-on: ubuntu-22.04  # Spécifie Ubuntu 22.04 pour éviter les migrations

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'  # Node.js 20 est stable et compatible

      - name: Install dependencies
        run: npm install

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install pronotepy
        run: pip install pronotepy

      - name: Run sync
        run: node auto-sync-complete.js
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          PRONOTE_USERNAME: ${{ secrets.PRONOTE_USERNAME }}
          PRONOTE_PASSWORD: ${{ secrets.PRONOTE_PASSWORD }}
